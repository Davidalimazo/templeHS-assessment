name: Code-Linter

on: push

jobs:
  code-linter:
    name: Lint code base
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: install NPM Packages
        run: yarn install
     
      - name: Build Project
        run: yarn build